---
import { markedParse } from "@configs/marked";

type Props = {
  id?: string;
  class?: string;
  raw: string;
};
const { id, class: className, raw }: Props = Astro.props;

const html = markedParse(raw);
---

<article id={id} class={className} set:html={html} />

<link
  rel="stylesheet"
  href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css"
  integrity="sha384-n8MVd4RsNIU0tAv4ct0nTaAbDJwPJzDEaqSD1odI+WdtXRGWt2kTvGFasHpSy3SV"
  crossorigin="anonymous"
/>

<style is:global>
  .katex {
    .mord.text :not(.mathnormal) {
      @apply font-sans text-base font-light;
    }
    .tag {
      @apply mx-2;
      & span {
        @apply !font-medium;
      }
    }
  }
</style>
